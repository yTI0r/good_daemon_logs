
if(NOT DEFINED LOGCONF_PREFIX)
    set(LOGCONF_PREFIX "/var/log")
endif(NOT DEFINED LOGCONF_PREFIX)

configure_file(logrotate.conf.in ${PROJECT_BINARY_DIR}/logrotate.conf)
configure_file(rsyslog.conf.in ${PROJECT_BINARY_DIR}/rsyslog.conf)

if(NOT DEFINED SYSTEMDCONF_TARGET_INSTALL_PATH)
    set(SYSTEMDCONF_TARGET_INSTALL_PATH ${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_BINDIR})
endif()

if(NOT DEFINED SYSTEMDCONF_EXEC_POSTFIX)
    set(SYSTEMDCONF_TARGET_INSTALL_PATH "")
endif(NOT DEFINED SYSTEMDCONF_EXEC_POSTFIX)
